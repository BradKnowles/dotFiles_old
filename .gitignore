# This .gitignore is non-standard, it is a white list.
# Files must be explicitly added as exclusions to be committed.

# Ignore all files in repo root
*.*

# Ignore all directories, and all sub-directories, and their contents
*/*

# Include these content files, using gitignore exclude syntax
!/*.cmd
!/*.md
!git/.gitconfig
!vim/.vimrc
!vim/.gvimrc
!SlickRun/SlickRun.ini
!SlickRun/SlickRun.srl
!SlickRun/Dependencies/
!SlickRun/Dependencies/*.*

# Include git configuration files
!/.gitignore
!/.gitattributes
